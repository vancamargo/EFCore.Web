@model EFCore.Dominio.HeroiBatalha

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>HeroiBatalha</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="HeroiId" class="control-label"></label>
                <select asp-for="HeroiId" class ="form-control" asp-items="ViewBag.HeroiId"></select>
            </div>
            <div class="form-group">
                <label asp-for="BatalhaId" class="control-label"></label>
                <select asp-for="BatalhaId" class ="form-control" asp-items="ViewBag.BatalhaId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
